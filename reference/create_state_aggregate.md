# Create state-level aggregate from district data

Create state-level aggregate from district data

## Usage

``` r
create_state_aggregate(df, end_year)
```

## Arguments

- df:

  Processed data frame (may already have state rows)

- end_year:

  School year end

## Value

Data frame with state aggregate added if not present
