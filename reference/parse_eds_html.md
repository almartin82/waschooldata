# Parse EDS directory HTML table

Extracts district directory data from the EDS HTML page using string
processing. The page contains a single HTML table with district-level
information.

## Usage

``` r
parse_eds_html(html)
```

## Arguments

- html:

  Raw HTML string from the EDS directory page

## Value

Data frame with parsed district data
